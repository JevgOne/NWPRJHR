# QA: TASK-109 re-check + TASK-099 zvoneček

**Datum:** 2026-07-22
**Kontrolor:** kontrolor
**Status: PASS — obě tasky OK**

---

## ČÁST 1: TASK-109 RE-CHECK

### Grep "poptáv" v celém projektu

**src/:** žádné výskyty ✅

**messages/:** jeden výskyt:
- `messages/cs.json:1972` — "poptávky" (tržní poptávka) — SPRÁVNĚ, nesmí být změněno ✅

### Opravené chyby z předchozího QA

**CHYBA 1 — email-templates.ts:204** ✅ OPRAVENO
- Dříve: `"Použijte kód při objednávce nebo poptávce na"`
- Grep "poptáv" v src/ → žádné výskyty → potvrzeno opraveno

**CHYBA 2 — route.ts:217** ✅ OPRAVENO
- Dříve: `` `${name} poptává ${items.length} položek.` ``
- Grep "poptáv" v src/ → žádné výskyty → potvrzeno opraveno

### Závěr TASK-109
✅ **KOMPLETNĚ HOTOVO** — žádné zbytky "poptávka" v uživatelsky viditelných textech

---

## ČÁST 2: TASK-099 PLNÁ QA

### 1. notifications.ts — inquiryId v entityKey union

```
src/lib/notifications.ts:401
entityKey: "orderId" | "reservationId" | "invoiceId" | "returnId" | "complaintId" | "inquiryId"
```
✅ `"inquiryId"` přidán do union type

### 2. orders/[id]/route.ts — reject maže notifikace

```
r210: deleteNotificationsForEntity("orderId", id).catch(() => {});
```
✅ Voláno po `createSalonNotification` v case "reject"
✅ Správná pozice — po odeslání rejection notifikace salonovi

Poznámka: funkce je importována na r14 — import OK ✅

### 3. inquiries/[id]/route.ts — CANCELLED maže notifikace

```
r6:  import { deleteNotificationsForEntity } from "@/lib/notifications"
r53: if (body.status === "CANCELLED") {
r54:   deleteNotificationsForEntity("inquiryId", id).catch(() => {})
```
✅ Import správný
✅ Podmínka správná (pouze při CANCELLED, ne při jiných stavech)
✅ `.catch(() => {})` — neblokující, konzistentní vzor

### 4. cron/expire-reservations/route.ts — expirace maže notifikace

```
r4:  import { createNotificationForRole, deleteNotificationsForEntity } from "@/lib/notifications"
r59-62: for (const oid of orderIds) {
          deleteNotificationsForEntity("orderId", oid as string).catch(() => {});
        }
```
✅ Import správný
✅ Loop přes orderIds — čistí notifikace pro každou expirovanou objednávku
✅ Voláno po `invalidateStockCache()` oblasti — správná pozice v kódu

### 5. NotificationBell.tsx — URL sjednoceny s NotificationsClient

```
r42: case "NEW_CONTACT":
r43:   return "/notifications";   ← sjednoceno (dříve "/inquiries")
r46: case "REGISTRATION":
r47:   return d.salonId ? `/salons/${d.salonId}` : "/registrations";  ← sjednoceno
```
✅ NEW_CONTACT: `/notifications` (shoduje se s NotificationsClient)
✅ REGISTRATION: `d.salonId ? /salons/{id} : /registrations` (shoduje se s NotificationsClient)

### TypeScript kompilace

```
npx tsc --noEmit → PASS (žádné chyby, žádné výstupy)
```
✅

---

## SOUHRN

| Kontrola | Výsledek |
|----------|----------|
| TASK-109: poptávka v src/ | ✅ žádné výskyty |
| TASK-109: cs.json:1972 netknuté | ✅ |
| TASK-109: email-templates fix | ✅ opraveno |
| TASK-109: route.ts:217 fix | ✅ opraveno |
| TASK-099: inquiryId v union | ✅ |
| TASK-099: reject maže notifikace | ✅ |
| TASK-099: CANCELLED maže notifikace | ✅ |
| TASK-099: expirace maže notifikace | ✅ |
| TASK-099: NotificationBell URL sjednoceny | ✅ |
| TypeScript kompilace | ✅ PASS |

**Obě implementace jsou kompletní a správné. Připraveno k deployi.**
