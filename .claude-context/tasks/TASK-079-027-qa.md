# QA: TASK-079 + TASK-027

**Datum:** 2026-07-22
**Kontrolor:** kontrolor
**Status: PASS — obě implementace správné**

---

## TASK-079 — Prodejní karta chybí info o produktu

### 1. SaleItemRow.tsx — processingType v interface + zobrazení

**Soubor:** `src/components/sales/SaleItemRow.tsx`

Interface `SaleItemData` (r23):
```typescript
processingType?: string;
```
✅ Přidáno jako optional field

Zobrazení (r73):
```tsx
{item.processingType && <span>{item.processingType}</span>}
```
✅ Podmíněné zobrazení (nezobrazí se pokud null/undefined)
✅ Správná pozice — mezi `texture` (r72) a `sku` (r74) v info řádku

### 2. NewSaleWizard.tsx — extrakce a předání processingType

**Soubor:** `src/app/(app)/sales/new/NewSaleWizard.tsx`

Interface `SaleItem` (r43):
```typescript
processingType?: string;
```
✅ Přidáno do interface

Extrakce z ProductOption (r142, r151):
```typescript
let processingType: string | undefined;
// ...
processingType = p.processingType;  // z produktu při nalezení varianty
```
✅ Inicializace jako `undefined` před loopu
✅ Extrakce z `p.processingType` při nalezení varianty

Předání do obou větví (r195, r215):
```typescript
// BY_PIECE větev (r195):
processingType,
// BY_GRAM větev (r215):
processingType,
```
✅ Předáno v BY_PIECE větvi
✅ Předáno v BY_GRAM větvi

**Poznámka:** API fallback větev (r157-168) `processingType` neextrahuje z API response — to je přijatelné, protože primární cesta (products loop) ji nastaví a fallback je pouze pro label. Není chyba.

---

## TASK-027 — Dashboard cache phantom data

### 1. db.ts — syncInterval=10

**Soubor:** `src/lib/db.ts:23`
```typescript
syncInterval: 10,
```
✅ Změněno z 60 na 10 (Turso sync 6x častěji)

### 2. returns/approve + reject — revalidateTag

**Soubory:**
- `src/app/api/returns/[id]/approve/route.ts:5,31`
- `src/app/api/returns/[id]/reject/route.ts:5,31`

```typescript
import { revalidateTag } from "next/cache";
// ...
revalidateTag("dashboard", "max");
```
✅ Import správný v obou souborech
✅ `revalidateTag("dashboard", "max")` v approve (r31)
✅ `revalidateTag("dashboard", "max")` v reject (r31)

### 3. reservations/[id]/route.ts — complete + cancel

**Soubor:** `src/app/api/reservations/[id]/route.ts:13,182,211`

```typescript
import { revalidateTag } from "next/cache";
// ...
// case "complete" (r182):
revalidateTag("dashboard", "max");
// case "cancel" (r211):
revalidateTag("dashboard", "max");
```
✅ Import správný
✅ `revalidateTag` v case "complete" — po vytvoření prodeje
✅ `revalidateTag` v case "cancel" — po zrušení rezervace

### 4. cron/expire-reservations/route.ts — podmíněný revalidateTag

**Soubor:** `src/app/api/cron/expire-reservations/route.ts:6,79-81`

```typescript
import { revalidateTag } from "next/cache";
// ...
if (expiredCount > 0 || expiredOrderCount > 0) {
  revalidateTag("dashboard", "max");
}
```
✅ Import správný
✅ Podmíněné volání — pouze když se něco expirovalo (správná optimalizace)
✅ Podmínka pokrývá obě větve (reservace i objednávky)

---

## TypeScript kompilace

```
npx tsc --noEmit → PASS (žádné chyby)
```
✅

---

## SOUHRN

| Kontrola | Výsledek |
|----------|----------|
| SaleItemRow: processingType v interface | ✅ |
| SaleItemRow: podmíněné zobrazení | ✅ |
| NewSaleWizard: processingType v interface | ✅ |
| NewSaleWizard: extrakce z produktu | ✅ |
| NewSaleWizard: BY_PIECE větev | ✅ |
| NewSaleWizard: BY_GRAM větev | ✅ |
| db.ts: syncInterval: 10 | ✅ |
| returns/approve: revalidateTag | ✅ |
| returns/reject: revalidateTag | ✅ |
| reservations: case "complete" revalidateTag | ✅ |
| reservations: case "cancel" revalidateTag | ✅ |
| expire-reservations: podmíněný revalidateTag | ✅ |
| TypeScript kompilace | ✅ PASS |

**Obě implementace kompletní a správné. Připraveno k deployi.**
